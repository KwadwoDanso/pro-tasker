PORT=5000
   MONGO_URI=your_mongodb_atlas_connection_string
   JWT_SECRET=your_long_random_secret_string
   CLIENT_URL=http://localhost:5173
    //Optional: paste an incoming-webhook URL to get a Slack ping when a task is
    //created or completed. Leave blank to disable. Requires Node 18+ (built-in fetch).
    SLACK_WEBHOOK_URL=
